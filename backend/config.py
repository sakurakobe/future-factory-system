"""
================================================================================
系统配置文件
================================================================================
所有系统级常量在此统一定义，包括：
- 数据库路径
- 上传文件存储目录
- 题库 XLSX 文件路径

修改配置只需在此文件中调整即可。
================================================================================
"""

import os

# ========================================
# 项目根目录（即 backend 目录所在路径）
# ========================================
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

# ========================================
# 数据库文件路径
# SQLite 数据库文件，首次运行时自动创建
# 默认在 backend 目录下生成 future_factory.db
# ========================================
DB_PATH = os.path.join(BASE_DIR, "future_factory.db")

# ========================================
# 上传文件存储目录
# 用户上传的附件文件保存在此目录下
# ========================================
UPLOAD_DIR = os.path.join(BASE_DIR, "uploads")

# ========================================
# 题库 XLSX 文件路径
# 默认从环境变量 XLSX_PATH 读取，如果未设置则使用默认路径
# 确保此文件存在，否则数据库初始化时会报错
# ========================================
XLSX_PATH = os.environ.get("XLSX_PATH", os.path.join(BASE_DIR, "未来工厂标准诊断评估题库.xlsx"))
